# 可在 OOBE 中保存到数据库；此环境变量仅作为兼容回退。不要提交真实密钥。
YZWC_GAME_API_KEY=replace-with-at-least-32-random-characters

# GitHub Release 自动部署使用。GitHub Actions 的 DEPLOY_TOKEN Secret 必须与此处一致，
# 至少 32 位且不要包含空白字符。部署包只会原子替换 .output，不会触碰 server/data。
YZWC_DEPLOY_TOKEN=replace-with-at-least-32-random-characters
# 默认部署根目录为 Nitro 进程的工作目录；生产环境工作目录不在项目根目录时必须显式填写。
# YZWC_DEPLOY_ROOT=/srv/youzai-world-api
# 默认在返回成功响应后退出，由 systemd/PM2/Docker 的自动重启策略拉起新版本；
# 若没有自动重启策略，可配置由部署进程启动的命令，例如：pm2 restart YouzaiWorldApi
# YZWC_DEPLOY_RESTART_COMMAND=pm2 restart YouzaiWorldApi

# 域名邮件（@mcyzw.top 收件）投递密钥，供 YouzaiWorldDonaimEmail 这个
# Cloudflare Email Worker 给 POST /api/inbound-mail 签名用。
# 与游戏 Api 密钥分开：Worker 被攻破也只能写入收件，碰不到 /api/game/*。
#
# 推荐在后台「站点设置」页填写（可点「随机生成」），保存到数据库；
# 此环境变量仅作为兼容回退——生产环境的 Nitro 运行时不会读这个 .env。
# 两处都没有有效值时收件投递接口返回 503，后台「域名邮件」页会一直是空的。
#
# 32 至 512 位、不含空白字符，须与 Worker 的 INBOUND_MAIL_KEY Secret 完全一致。
# 生成：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
YZWC_INBOUND_MAIL_KEY=replace-with-at-least-32-random-characters

# 后台发送域名邮件时使用的默认 HTML 模板。默认依次查找进程工作目录下的 reply.html
# 和其上一级目录的 reply.html；生产部署目录不一致时可显式指定绝对路径。
# YZWC_REPLY_TEMPLATE_PATH=/srv/youzai-world/reply.html

# 后台「服务器管理」页调用 MCSManager 面板用的凭据。
# ApiKey 在面板右上角用户信息页生成，权限与该面板账户完全一致（可发命令、改文件、停服），
# 因此推荐在后台「站点设置」页填写并保存到数据库；此处仅作兼容回退，
# 生产环境的 Nitro 运行时不会读这个 .env。两处都没配时「服务器管理」页会提示去设置。
# 地址只支持 http/https，不要带查询串。整实例备份使用 ElementsPanel 节点的备份插件配置。
# YZWC_MCSM_BASE_URL=http://127.0.0.1:23333
# YZWC_MCSM_API_KEY=replace-with-your-mcsm-api-key

# Cloudflare Turnstile 登录保护。站点密钥可以公开，服务端密钥不得提交到仓库。
NUXT_PUBLIC_TURNSTILE_SITE_KEY=replace-with-your-turnstile-site-key
TURNSTILE_SECRET=replace-with-your-turnstile-secret
# 生产环境只填写实际前端域名，不要包含 localhost 或 127.0.0.1。
TURNSTILE_HOSTNAMES=replace-with-allowed-hostname

# 官网聊天区专用的 Turnstile 凭据（与上面后台登录那套分开）。
# 聊天区 widget 跑在主站域名下，后台登录 widget 只允许 api 域名；
# 混用会导致 siteverify 回传的 hostname 过不了允许域名校验，
# 表现为“前端显示验证通过、发送却提示人机验证失败”。
# 不配置时会回退到上面后台那一套。
TURNSTILE_CHAT_SITE_KEY=replace-with-chat-turnstile-site-key
TURNSTILE_CHAT_SECRET=replace-with-chat-turnstile-secret
# 必须与 Cloudflare 控制台里该 widget 的 Allowed domains 一致。
TURNSTILE_CHAT_HOSTNAMES=mcyzw.top,www.mcyzw.top

# 聊天区 IP 归属地解析。默认调用第三方中文 IP 库取省市级归属地并按 IP 哈希缓存 7 天；
# 置为 1 可关闭外呼，仅回退到 Cloudflare 的 cf-ipcountry 国家级信息。
# YZWC_IP_GEO_DISABLED=1

# 后台「账户装扮」页的正版皮肤回退。默认按玩家代号查 Mojang 档案（结果缓存 6 小时）
# 并代理拉取官方材质；置为 1 可关闭全部对 Mojang 的外呼，只展示离线上传的外观。
# YZWC_MOJANG_DISABLED=1

# 后台密码和登录入口默认在首次访问根目录时设置。
# 如需无人值守初始化，可同时提供下面两个变量。
# YZWC_ADMIN_PASSWORD=replace-with-a-strong-admin-password
# YZWC_ADMIN_USERNAME=admin
# YZWC_ADMIN_ENTRY=replace-with-12-to-64-random-characters

# 游戏会话最长 24 小时，默认 12 小时。
YZWC_GAME_SESSION_TTL_SECONDS=43200

# 状态后台从独立 Cloudflare Worker 获取当前状态和 72 小时历史。
YZWC_STATUS_WORKER_URL=https://status.mcyzw.top/api/status
